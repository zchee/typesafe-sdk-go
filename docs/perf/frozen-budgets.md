# Frozen budgets (W0.6)

Written by the W0.6 verifier lane (verify-p0) at 2026-09-25 20:06:37 JST, against `main` @ `d47b403`; the AC-P4 ordering row was updated by the lead for the W0.4b round-2 rows (fdea850). Every clause is filled. Relative to `2366cce`, where every gate was re-run, d47b403 changes only `_spikes/s-t` and the ledger. The W0.4b rows were reproduced from their raw files with `median.py`, and by a fresh local run at d47b403.
Accepted by the W0.6 critic lane (critic-p0) with `CRITIC P0: APPROVE`; landed by the lead at 2026-09-25 20:10:37 JST (from `date`) with the critic's two wording fixes (A3: MiB values; A4: growth-call bytes). The AC-P6 time clause against a sonic-based naive comparator, left PENDING the owner at W0.6 (critic finding B6), was decided by the owner in G3 (a): the comparator of record is a sonic-based naive client (`sonic.Marshal` and `sonic.Unmarshal` into `map[string]any`, `internal/testsupport/naive`, built by W5.1), encoding/json is reported only, and the clause is asserted on amd64 and recorded on arm64 (K18).
W3.4, the last wave of Phase 3, re-measured the client on `main` @ `1f694b0` and froze both AC-P6 rows below: N = 14 and the time clause under G3 (a) (ledger rows W3.4-01 to W3.4-10, 2026-09-26 13:13–13:24 JST on (M) and 04:14–04:16 UTC on (L)).

W5.2 added the Test column, the test that asserts or records each row (ledger `## W5.2`), and the AC-P5 rows (vi) and (vii).

Row IDs refer to [`docs/perf/ledger.md`](perf/ledger.md); rulings refer to `.omc/handoffs/rulings.md`. G2 is the owner's W0.6 decision.

The verifier reproduced every number below from the committed raw files under `_spikes/*/results/`. Two scripts did the checking:

- `xcheck3.py` compared 859 cells of the W0.3 tables, allocation and timing, on (M) and (L).
- `xcheck2.py` compared 136 cells of the W0.5 tables.

The only differences are cells the ledger re-sources to the `duplicates` re-measure (W0.3-21/-22), plus one print artefact ("128.0 ns" against "128 ns").

A fresh run at `2366cce` on (M), under the lock with `GOEXPERIMENT=nosimd,noruntimesecret` and started 2026-09-25 19:43:46 JST, printed the same S-C1 CALL, STAGE, ITEM and MEM lines, and the same S-E1 and S-D1 allocation counts. Allocation counts are the same on (M) and (L) unless a row says otherwise.

The Verdict column compares each frozen value with the plan's text:

- **tightens**: the frozen value is stricter than the plan.
- **matches**: the frozen value equals the plan.
- **LOOSENS**: the frozen value is weaker than the plan. Every loosening below is owner-approved in G2.
- **PENDING**: no value is frozen yet.

| Criterion | Clause | Test | Frozen value | Provenance | Plan text | Verdict |
| --- | --- | --- | --- | --- | --- | --- |
| AC-P1 | steady-state encode allocations (NF1) | `TestAllocEncode` (`alloc_encode_test.go`) (every kind at 1 KiB, 64 KiB, 1 MiB, 6 MiB); the state encode alone: `internal/codec` `TestEncodeStateAllocations` (`internal/codec/encode_test.go`) (1 KiB) | `E(kind) + B` per call on a warm pool, while the scratch stays within the 8 MiB ceiling. Up to 6 MiB on both hosts: bare `string` 2 (E 1 + B 1); boxed `any` 1; `json.RawMessage` 1; RawJSON appended verbatim 0. `*struct` 1 and flat `map[string]any` 2: at ≤ 1 MiB on both hosts, and at 6 MiB on (L). **At 6 MiB on (M): `*struct` 22 (27 601 168 B) and flat map 22 (27 597 808 B).** sonic grows the scratch past the ceiling on arm64 (9,020,409 B = 8.60 MiB and 9,181,884 B = 8.76 MiB after the call), so it is dropped after every call and every call pays growth (R22). A state holding m maps costs 1 + m (nested maps: 9 at 1 KiB up to 45 010 at 6 MiB, on both hosts; documented, a W5.3 candidate). At 9 MiB every kind exceeds the ceiling on both hosts and is reported, not budgeted | "S-E1 pooled encode" table, W0.3-04 (M) and W0.3-02 (L) (`ENC` lines of `s-e1/results/alloc-{M,L}.txt`); R22, R24, R24b | "≤ `E_sonic + B`, `E_sonic` = 1; B = 1 when the state is a bare `string`" | matches for string, boxed and RawMessage, and for `*struct` within the ceiling; tightens for RawJSON; **LOOSENS for map states: owner-approved (G2 b)**. `*struct` and flat-map states of about 6 MiB on arm64 are outside the steady-state budget (R22) |
| AC-P1 | steady-state encode allocations inside a request body (W5.2, ruling R110) | `TestAllocEncode` (`alloc_encode_test.go`) | `E(kind) + B` per body encode on a warm pool, at 1 KiB, 64 KiB, 1 MiB and 6 MiB for every kind, on both hosts: bare `string` 2, boxed 1, bare RawJSON 1, boxed RawJSON 0, `json.RawMessage` 1, `*struct` 1, flat map 2, nested map 1 + m (9, 471, 7 503, 45 010); at most 24 B above E_sonic's bytes. **The one exception is `*struct` at 6 MiB on arm64**: its growth ends at 8.86 MiB, past the ceiling, so each call costs 23 allocations, 30 469 392 B (recorded; the row above measured S-E1's struct, 22). The flat map at 6 MiB ends at 7.51 MiB on arm64, within the ceiling, and is asserted (2); on amd64 both end at 6.44 MiB. In CI the arm64 image (xcode-27) records `*struct/6MiB` and asserts the map; ubuntu-26.04 and windows-2025 assert both (K18) | **W5.2-01 (M), W5.2-02 (L)**; CI dispatch 36225961754 at a02eb57 (all three images); R22, R62, R110 | "≤ `E_sonic + B`" | **tightens** the row above, which is kept as S-E1 measured it (the encoder alone): the exception narrows from `*struct` and flat map to `*struct` alone. Narrowing an exception tightens the freeze, so no owner question (R110 (1)) |
| AC-P1 | encode bytes | `TestAllocEncode` (`alloc_encode_test.go`) | warm-pool calls ≤ 112 B/call above sonic's own. Growth calls are recorded, not bounded: growing from a 4 KiB scratch, string, boxed, RawJSON and RawMessage cost 1.00–1.13 × body (the sequence's call 22, growing 6 → 9 MiB, costs 12,591,120 B = 1.33 ×, `s-e1/results/alloc-M.txt:78`); `*struct` and flat map about 4.3 × (M) and 5.2 × (L). On (M), a 6 MiB `*struct` or flat-map state never reaches the warm state (its scratch is dropped every call) and costs about 27.6 MB per call | W0.3-02 (L), W0.3-04 (M); R24b | "Bytes ≤ 1.05 × body + 4 KiB above sonic's own" | tightens on warm calls; **LOOSENS on growth calls: owner-approved (G2 b)** |
| AC-P1 | retained scratch | `internal/codec` `TestBodyScratchCeiling` (`internal/codec/scratch_test.go`); `TestAllocEncode` (`alloc_encode_test.go`), `TestAllocScratchSequence` (`alloc_sequence_test.go`) | ceiling 8 MiB (`codec.ScratchCeiling = 8 << 20`; `TestBodyScratchCeiling`) | code at 2366cce | "scratch ≤ 8 MiB" | matches |
| AC-P1 | mixed-size sequence (`TestAllocScratchSequence`) | `TestAllocScratchSequence` (`alloc_sequence_test.go`); call 23: `internal/codec` `TestNewBodyAllocations` (`internal/codec/scratch_test.go`) | assertions start at call 2 (R22b). RawJSON state, calls 2..32: `0×9 \| 1 \| 0×10 \| 1 \| 1 \| 0×9`. Boxed-string state, calls 2..32: `1×9 \| 2 \| 1×10 \| 2 \| 2 \| 1×9`. Exactly one drop, at call 22; call 23 = base + 1. Call 1 is recorded: 4 and 8 | W0.3-02, W0.3-04; R22, R22b; fresh run identical | "exactly one scratch is dropped (call 22) … calls 12-21 and 24-32 match the single-size numbers"; "call 23 pays one extra allocation" | matches, scoped to RawJSON and boxed string. `*struct` and flat-map states on (M) also drop at call 11 and are recorded, not asserted (R22) |
| AC-P1 | g₆ / g₉ | `TestAllocScratchSequence` (`alloc_sequence_test.go`) | asserted kinds: boxed 2 / 2, RawJSON 1 / 1. Recorded only: string 3 / 3; RawMessage 2 / 2; `*struct` 21 / 22 (M) and 30 / 32 (L); flat map 21 / 22 (M) and 31 / 33 (L) | "S-E1 growth" table (W0.3-02, W0.3-04) | "growth costs ≤ g₆/g₉ from S-E1" (plan probe: 5 / 7) | tightens |
| AC-P2 | decode allocations per fixture (a1) | `TestAllocDecodeFixtures` (`alloc_decode_test.go`) (every fixture that decodes; "≤ 0.5 × naive" on `result.json`) | result 4, type-last 4, **duplicates 17**, result-20 24, score-flood-mini 21, escaped-names 10, escaped-member-names 30, structured-legend 12, deviation-lone-surrogate 14, unknown-answer-type 1, parity-big-exp-unknown 1, no-answers 0, structured-legend-flood-1k 91, structured-legend-flood-10k 686. The ceiling of 8 on `result.json` stays in case W2.0's API wrapper adds allocations | W0.3-11 (L), W0.3-12 (M); duplicates: W0.3-21 (L), W0.3-22 (M), 825 B, 17/5336; R24, R24b; **duplicates = 17 owner-approved (G2 c)** | "plain 3-answer fixture ≤ 8 allocations (target 4), ≤ 0.5 × naive; 20-answer and escaped fixtures reported" | tightens: 4 meets the target, and the 20-answer and escaped fixtures now have budgets. "≤ 0.5 × naive" is in force: asserted by `TestAllocDecodeFixtures` since W5.2 against `naive.Sonic`'s decode of the same body in the same run, 4 / 26 = 0.154 (M) and 4 / 40 = 0.100 (L) (W5.2-03, -04) |
| AC-P3 | typed decode allocations, `DecodeAs[T]` against the `Answers()` decode of the same fixture in the same run | `TestAllocTypedDecode` (`alloc_typed_test.go`) | `result.json` into `reviewAnswers`: **`DecodeAs` 0 allocations, 0 B** (W5.3, owner ruling R116: the answers are stored at the fields' offsets, `decodeas_store.go`, and the `T` stays on the stack), pinned exactly, against the `Answers()` decode's 4/688 B; `Ask` = `SystemOne` + 0 (22/2 648 B). Before W5.3 (W4.2 to W5.2): `DecodeAs` 1/144 B, the `T` moved to the heap by `reflect.Value.Interface` (R112) | W4.2-01, W5.2-03/-04; the typed store's rows of the ledger's `## W5.3`; R112, R116 | "`DecodeAs[T](resp)` allocates fewer times than the `Answers()` decode of the same fixture in the same run" | tightens: 0 against 4 |
| AC-P4 | transport design | `internal/h2gate` `TestFanOut` (`internal/h2gate/gate_test.go`), `TestWaiterFallThrough` (`internal/h2gate/gate_test.go`), `TestLeaderVanish` (`internal/h2gate/gate_test.go`) | **option (iv-b) per G2, worded per R29 and R29b**: strict mode, the cold-start gate, and a header-write token. **There is one token per client transport**, not one per connection: a per-connection token would re-open F1, because the stdlib reserves the stream before `GotConn`. Each request acquires the token before `RoundTrip` and releases it at `WroteHeaders`, except that the first request on each **new** connection releases it at its response headers. That hold is bounded by the gate's wait bound, `connectTimeout` + `TLSHandshakeTimeout`: **20 s with both at their 10 s defaults** (R29b). After it, waiters fall through and the client still uses 1 connection. So K19 ("no gate state can wedge the client") holds under `WithNoTimeout`, and W2.2 tests the fall-through. K21 (stdlib-internal replays bypass the token) stays documented; W0.4b did not exercise it, because no stream was refused or replayed. K22 (new): each cold burst on a new connection pays the latency of one leader response | G2, R29, R29b, F1-c; W0.4 "Mitigations compared"; W0.4b results | §6.3 strict mode plus the gate | owner-approved design change |
| AC-P4 | cold 64 → 1 connection | `internal/h2gate` `TestFanOut` (`internal/h2gate/gate_test.go`) | 1 connection in 50/50 bursts on both hosts: with the gate (W0.4), and **with FirstHold, (iv-b), as frozen** (W0.4b). With 64 calls at 50 ms service time, FirstHold also opens 1 connection with 64/64 ok | W0.4-05 (L), W0.4-14 (M); **W0.4b-01 (L), W0.4b-03 (M)**; W0.4b-02 and -04 (`64vs0/strict/token-first/service50ms`) | "Cold 64 → 1 connection" | matches |
| AC-P4 | warm → 0 new connections | `internal/h2gate` `TestFanOut` (`internal/h2gate/gate_test.go`) | 0 in 50/50 on both hosts, with the gate and with FirstHold. The warm burst also keeps the original clause (every request on the wire before any response) in 50/50 | W0.4-05, W0.4-14; W0.4b-01, W0.4b-03 | "warm → 0" | matches |
| AC-P4 | 200 calls against MAX_CONCURRENT_STREAMS=8 all succeed within the per-call deadline on 1 connection (R29) | `internal/h2gate` `TestFanOut` (`internal/h2gate/gate_test.go`) | **Asserted**: 200/200 succeed within the 2 s per-call deadline, on 1 connection. Measured with FirstHold: 200 (200-200) ok, 1 (1-1) accept, wall **271.7 ms (L) and 293.0 ms (M)** (W0.4b); the W0.4 rows gave 271.5 and 291.8 ms. The server's stream high-water mark was 8, so the limit was reached. 64 vs 4 behaves the same: 64/64 on 1 connection, 176.0 ms (L) and 190.4 ms (M). **Recorded, not asserted**: the count of refused streams. It was 0 in all 20 W0.4b runs on each host and in the W0.4 matrix, but the loaded (M) stall runs (W0.4-16) saw 2 and 1 with no missed deadline, so an assertion of 0 would flake. Stall rate at a 500 ms deadline: 0 of 20 repetitions on both hosts. The broken strict configuration of the plan also shows 1 connection, but only 1 (1-3) (L) and 8 (1-8) (M) of the 200 succeed | **W0.4b-02 (L), W0.4b-04 (M)** (`s-t/results/{l,m}-st1b-firsthold.txt`); W0.4-06, -07, -15, -16 | "200 vs limit 8 → 1" | matches under (iv-b). R29's wording adds the success requirement, a tightening of the plan's text |
| AC-P4 | ordering | `internal/h2gate` `TestFanOut` (`internal/h2gate/gate_test.go`) (the negative control: `TestRecordFanOut` (`internal/h2gate/record_test.go`)) | **Frozen, per G2 and R29 (4)**: no waiter's request is written before the leader's response headers; after them, all 63 waiters' requests are on the wire on the same connection before any waiter's response (5 s guard). Measured with FirstHold, 50 bursts per host (5 runs × 10): the clause held 50/50 on both hosts. Its parts held 50/50 each: (a) the first request at the handler is the leader's; (b) the leader's response arrives before any other HEADERS; (c) the guard released nothing; (d) every request is on connection 0. Margin for (b), from the leader's response to the first other HEADERS: at least 0.036 ms (L) and 0.016 ms (M). Negative control, the plain token (iv-a), passed only 37/50 (L) and 22/50 (M), so the check discriminates, but only by chance on loopback. **R29b/R29c: the frozen test delays the leader's response by at least 5 ms (W2.2 uses 20 ms) and asserts (b) from client-side traces (the leader's first response byte precedes every other caller's WroteHeaders); at 5 ms the plain-token control fails 50/50 on both hosts while FirstHold passes 50/50 (W0.4b-06/-08, fdea850).** Bounded hold (R29 (3), W0.4b-07/-09): with the gate wait bound at 500 ms and the leader's response held 1 s, the waiters' HEADERS go out at 500.9 ms (L) / 500.3 ms (M), 64/64 ok on 1 connection; without the bound they wait the full second. Cold-burst cost at 200 ms service (W0.4b-10): 202.7 vs 403.3 ms (L), 204.2 vs 405.2 ms (M): one leader response per cold burst (K22). W2.2 re-tests the fall-through against the production transport | **W0.4b-01 (L), W0.4b-03 (M)** (`s-t/results/{l,m}-st1-fanout-w04b.txt`); **W0.4b-06..10** (fdea850: `{l,m}-st1-fanout-w04b2.txt`, `{l,m}-st1b-firsthold2.txt`); `median.py` reproduces every count and latency; fresh local run at d47b403: FirstHold 10/10, control 7/10 | "every request is on the wire before any response" | **LOOSENS: owner-approved (G2 a); frozen** |
| AC-P4 | waiter latency (recorded, not asserted) | `internal/h2gate` `TestFanOut` (`internal/h2gate/gate_test.go`) (logged), `TestRecordFanOut` (`internal/h2gate/record_test.go`) | FirstHold, (iv-b), waiters' wire time p50 / p99: (L) 1.451 / 2.537 ms, (M) 1.064 / 1.928 ms; gate alone (W0.4): (L) 1.271 / 2.357 ms, (M) 1.146 / 1.898 ms. Leader's write to the first waiter's write, p50: 0.1 ms against 0.057 ms with the plain token (L), and 0.084 against 0.039 ms (M). **K22**: a cold burst's waiters wait for the leader's response. 64 calls at 50 ms service take 103.3 ms against 52.7 ms with the gate alone (L), and 103.9 against 53.2 ms (M) | W0.4b-01, -02, -03, -04; W0.4-05, W0.4-14 | "waiter p50/p99 recorded in the ledger, not asserted" | matches |
| AC-P5 | scope | `TestMemStatsCap` (`alloc_call_test.go`); over a real connection, without the bounds: `TestResponseCapOverTheWire` (`alloc_wire_test.go`) | bounds apply **per attempt**; `TestMemStatsCap` runs with `Retry(NoRetry())` and asserts cases (i)–(v) (R26b). The first read buffer (R27) is `min(Content-Length, 256 KiB)` for a declared body and 4 KiB, doubling, for an undeclared one | R26, R26b, R27 | "TotalAlloc delta for one call" | matches under NoRetry. R27's 4 KiB first buffer for undeclared bodies tightens small chunked calls (6 200 B against 264 248 B) |
| AC-P5 | (i) `Content-Length: 16 MiB`, 10 bytes sent → `io.ErrUnexpectedEOF` | `TestMemStatsCap` (`alloc_call_test.go`) | TotalAlloc ≤ 327 680 B (256 KiB + 64 KiB). Measured 263 448 B, 15 mallocs; read stage 262 144 B | W0.5-01 (M), W0.5-05 (L); R26 | "< 1 MiB" | tightens |
| AC-P5 | (ii) declared 16 MiB + 1 → `*ResponseTooLargeError`, nothing read | `TestMemStatsCap` (`alloc_call_test.go`); `TestResponseCapOverTheWire` (`alloc_wire_test.go`) (the class, nothing read, one attempt) | ≤ 65 536 B. Measured 1 328 B, 15 mallocs; read stage 24 B | W0.5-01, W0.5-05; R26 | "refused before reading, delta < 1 MiB" | tightens |
| AC-P5 | (iii) undeclared 16 MiB + 1 → `*ResponseTooLargeError` at cap + 1 | `TestMemStatsCap` (`alloc_call_test.go`); `TestResponseCapOverTheWire` (`alloc_wire_test.go`) (the class, cap + 1 read, one attempt) | ≤ 2 × cap + 65 536 B = 33 619 968 B. Measured under R27's policy: 33 551 664 B, 28 mallocs (margin 68 304 B) | W0.5-01 (M), W0.5-05 (L), `split-256K-4K` column; R26b | "≤ 2 × cap + 1 MiB" | tightens |
| AC-P5 | (iv) declared 16 MiB, accepted | `TestMemStatsCap` (`alloc_call_test.go`) | ≤ 2 × cap + 65 536 B. Measured 33 294 392 B, 26 mallocs | W0.5-01, W0.5-05; R26 | – (new clause) | new clause (tightening) |
| AC-P5 | (v) undeclared 16 MiB, accepted | `TestMemStatsCap` (`alloc_call_test.go`) | ≤ 2 × cap + 65 536 B. Measured under R27's policy: 33 552 440 B, 32 mallocs (margin 67 528 B); the largest delta under R27 | W0.5-01 (M), W0.5-05 (L), `split-256K-4K` column; R26b | – | new clause (tightening) |
| AC-P5 | (vi) declared `result.json`, accepted | `TestMemStatsCap` (`alloc_call_test.go`) | ≤ 65 901 B = 365 B, the buffer `readBody` allocates for a declared body (Content-Length + 1: the 364-byte body and the byte that proves its end), + 65 536 B, the allowance (ii) freezes for everything a call allocates besides the response buffer (R26). Measured 2 392 B = 384 B (the 365-byte buffer's size class; ITEM `readBody` 1/384) + 2 008 B (the rest of the call over the Recorder, the Recorder's response included), 20 mallocs, on (M), (L) and the three CI images (maximum 2 392 B; dispatch 36225961754 at a02eb57): margin 63 509 B, 27.6 ×. The bound is an order of magnitude above the steady state on purpose: it bounds the declared-size trap, a buffer sized from the length a server declares (up to the 16 MiB cap) rather than the bytes it sends, which would add hundreds of kilobytes and fail; the steady-state count is AC-P6's exact pin. Every run is checked (R92 m-1) | **W5.2-03 (M), W5.2-04 (L)**; CI 36225961754 (a02eb57); R26, R27, R110 (2) | – (new clause) | new clause (tightening), W5.2, accepted by R110 (2) |
| AC-P5 | (vii) undeclared `result.json`, accepted | `TestMemStatsCap` (`alloc_call_test.go`) | ≤ 69 632 B = 4 096 B, the first buffer `readBody` allocates for an undeclared body (R27; the 364-byte body fits, so it never grows), + 65 536 B, (ii)'s allowance. Measured 6 104 B = 4 096 + 2 008 B, 20 mallocs, on (M), (L) and the three CI images (maximum 6 152 B on xcode-27, +1/+48 B once in five runs: K32's type-assertion cache): margin 63 480 B over the maximum, 11.3 ×. As for (vi), the bound catches a buffer sized from anything but the bytes read, not steady-state noise. Every run is checked (R92 m-1) | **W5.2-03 (M), W5.2-04 (L)**; CI 36225961754 (a02eb57); R26, R27, R110 (2) | – (new clause) | new clause (tightening), W5.2, accepted by R110 (2) |
| AC-P6 / NF3 | SDK allocations above the floor, N | `TestAllocWholeCall` (`alloc_call_test.go`) (q3 pinned; q20 recorded) | **FROZEN N = 14** (W3.4) for q3: 3 questions, a 1 KiB boxed-string state, the discarding Recorder answering `result.json`, `DefaultRetry()` in force with its first attempt succeeding, no per-call options, the default logger (`slog.DiscardHandler`). Floor 8/640 B (the Recorder's round trip 7/624 and E_sonic 1/16); call 22/2 648 B; **SDK-own 14/2 008 B**, identical on (M) and (L). Composition as built: `context.WithTimeout` 4 (272 B), decode 4 (688 B: the visitor's three fold slices and `wire.Answers.Grow`), `readBody` 1 (384), `*http.Request` from `WithContext` 1 (320), the attempt's URL copy 1 (144), `*SystemOneResponse` 1 (112), `codec.Body.Open`'s reader 1 (64), the `GetBody` method value 1 (24); header map 0 (the first attempt sends the client's template itself, R77). Against R28's 15: header map −2 (R77), URL copy +1 (R66 NIT 5). W5.3 candidates (the Rust port's 12): R79 NIT 3's four (the 4 of `context.WithTimeout`, the `GetBody` method value, `new(SystemOneResponse)`, the URL copy) and W2.3 finding 2's (one slab for the fold slices, −2). Stability: over 100 runs per host the call is at its minimum in 100 (M) and 99 (L); the one other (L) run cost 23/2 696, own 15 (K32-res: the runtime's type-assertion cache). So the pin in `TestAllocWholeCall` asserts own == 14 and the floor 8/640 exactly on the minimum that 3 of 5 runs share, not on every run. Recorded, not in N: a real transport +1 per attempt (the context's Done channel); q20 SDK-own 34/9 248 B (decode 24); a call that passes any option +1/+160 B (`callOptions` on the heap, R97-corr): `Retry` +1/160, `Timeout` +2/176, `Model` +3/192, `ExtraBody` +2/192, `Header` +7/720, all five +11/784; `WithLogger` at INFO +1/48, at DEBUG +3/96 (W3.3) | **W3.4-01 to -06** (`_spikes/w3.4/render.py`); W2.3-01/-03, W2.5-01/-02, W3.2-01 to -06, W3.3-01 to -04; W0.5-01, W0.5-05; R28, R77, R79, R97-corr | "≤ N … (Rust: 12), provisional until the end of Phase 3; < 0.5 × naive allocations" | **frozen; tightens** R28's provisional 15 by 1 (plan: N is set by S-C1). SDK-own / naive-own against the sonic comparator (naive allocs/op minus the floor): q3 14/46 = 0.304 (M) and 14/60 = 0.233 (L); q20 34/119 = 0.286 (M) and 34/239 = 0.142 (L); all < 0.5: matches |
| AC-P6 | time | `internal/benchmark` `BenchmarkCall` (`internal/benchmark/call_test.go`) (a benchmark, not a test) | **FROZEN per G3 (a)** (W3.4): `call/sdk` ns/op < `call/naive` ns/op on the q3 rows of `internal/benchmark`'s `BenchmarkCall` (G5), where `call/naive` is `naive.Sonic`, the sonic-based naive client (`sonic.Marshal` and `sonic.Unmarshal` into `map[string]any`, the SDK's own request bytes and header template, the same Recorder; W5.1). Asserted on amd64 (L); recorded on arm64 (M) under K18; the q20 rows are recorded (R101 (b)). (L), medians of 10: q3 **6.205 against 6.969 µs, 0.890** (0.898 by minima; W5.1-03: 6.104 against 6.885 µs, 0.887): holds; q20 25.89 against 26.42 µs, 0.980 (margin 2.0 %; W5.1: 0.983), a W5.3 input. **Re-measured after W5.3's K36 (one scan of the body), W5.3-03: q3 5.610 against 6.863 µs, 0.817; q20 22.03 against 25.93 µs, 0.850 (margin 15.0 %); (M), W5.3-05, recorded: q3 1.158, q20 1.594.** (M), recorded: q3 5.126 against 4.022 µs, 1.274 (W5.1-01: 1.333); q20 24.87 against 14.32 µs, 1.737 (W5.1: 1.880); the arm64 decode (K23), a W5.3 target. encoding/json (`naive-json`), reported only: q3 0.367 (L) and 0.567 (M); q20 0.335 and 0.622 (W0.5's prototype, R28: q3 0.35 and 0.52, q20 0.31 and 0.59). CodSpeed's pull-request run (AC-P7) is W5.4's | **W3.4-08 (M), W3.4-09 (L)** (`call-M-2.txt`, `call-L.txt`, `benchstat-call-*.txt`); W3.4-07 (M, under more load: 1.304 and 2.008); W5.1-01 (M), W5.1-03 (L); W0.5-04, W0.5-08; G3, R28b, R101, K18, K23 | "`call/sdk` ns/op < `call/naive`" | **frozen: matches on amd64**, the gate (G3 (a)); arm64 recorded under K18, where the clause does not hold (1.27–1.30×, K23 → W5.3) |
| AC-P8 | time, 10⁴ vs 10³ flood | `TestLinearityFloodTime` (`alloc_decode_cases_test.go`; every build, `-race` included) | ratio ≤ 15. Measured for a1: `TestLinearity` 9.70–10.07 (M) and 9.98–10.26 (L); benchmark medians 10.09 (M, re-measured) and 9.75 (L) | W0.3-17 (L), W0.3-18 (M); W0.3-13 (L), W0.3-14 (M, re-measured at cc1524a per R25) | "≤ 15 × the 10³ flood" | matches |
| AC-P8 | lazy-pass allocations | `TestLazyPassAllocations` (`internal/codec/decode_bench_test.go`; the lazy pass is unexported, R110 (3); CI: the allocation-budget step's codec list, non-race, on all three images); the members visited (1 011, 10 011): pinned from the pass's own count in `TestLazyPassAllocations` and counted from the fixture with encoding/json in `TestLinearityFlood` (`alloc_decode_test.go`) | ≤ 20 + ⌈members / 15⌉ + escaped keys iterated + 1 arena (c₀ = 20, c₁ = 1/15). Measured, arena included: 87 at 1 011 members and 682 at 10 011 (headroom 2 and 7); duplicates 10 at 18 members with 1 escaped key (bound 24) | W0.3-11, W0.3-12, W0.3-21, W0.3-22; R24 | "≤ c₀ + c₁ × (members visited), with c₀, c₁ frozen at W0.6" (probe: 82 / 677) | matches the plan's form, plus one allocation per escaped key |
| AC-P8 | allocation ratio, 10⁴ : 10³ | `TestLinearityFlood` (`alloc_decode_test.go`, whole decode); `TestLazyPassAllocations` (`internal/codec/decode_bench_test.go`, lazy pass) | ≤ 12. Measured: 7.54 (whole decode, 91 → 686) and 7.92 (lazy pass, 86 → 681) | W0.3-11, W0.3-12 | "≤ 12" | matches |
| AC-Q3 | race and fuzz | ci.yaml `go test -race` step; the fuzz targets join at W6.1 | `-race` green at d47b403: CI 36126929459 on 3 images; locally at 2366cce, 2026-09-25 19:43:39 JST (code outside `_spikes` has not changed since). The fuzz clause (4 targets, 60 s each in CI) joins at W6.1 | CI; local | "`-race` green; four fuzz targets 60 s each in CI" | in force (race); fuzz PENDING W6.1 |
| AC-Q4 | seams and D1 refusal | `internal/codec` `TestSeam*` (`internal/codec/seam_test.go`); ci.yaml "D1 refusal off the support matrix" | seam tests pass. The JIT files `[api.go rawmessage.go sonic.go]` compile. `vet` and `build` refuse with the D1 identifier on 386, riscv64, `-tags go1.28` and js/wasm. gotip refuses on tip | CI 36126929459 at d47b403 (lint; the same steps passed in 36113167206 at 2366cce); gotip 36111833315 (`gotip.yaml` unchanged since then); local, 2026-09-25 19:43:44 JST | AC-Q4 text | in force (matches) |

Notes:

1. **B1 is closed.**
   - 806f34b updates the spike's expected body, and `TestModuleDuplicates` passes for a1, a2 and b (local run at 2366cce).
   - Rows W0.3-21/-22 record `duplicates` at 17/5336 on both hosts; the raw files are `alloc-*-base31f2986.txt`.
2. **B2 is closed.** 93a4a9b tracks all four bench files, plus W0.5's two.
   - (L) files: identical to the surviving copies. `s-e1/results/bench-L.txt` has sha256 `d5ce80a8…`, and `s-d1/results/bench-L.txt` matches byte for byte (`cmp`).
   - Headers: every file's header matches its row (lock time, load, base).
   - W0.3-06 and W0.3-14 were re-measured at cc1524a with baseline ToolTags, and the rows note the original runs of record.
   - `_spikes/.gitignore`'s `!bench*.txt` beats the global `bench*.txt` rule: `git check-ignore -v` names `_spikes/.gitignore:5` for new bench files, while `bench.txt` stays ignored.
   - Every (M) and (L) timing cell of the W0.3 tables reproduces from these files.
