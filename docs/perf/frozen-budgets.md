# Frozen budgets (W0.6)

Written by the W0.6 verifier lane (verify-p0) at 2026-09-25 20:06:37 JST, against `main` @ `d47b403`; the AC-P4 ordering row was updated by the lead for the W0.4b round-2 rows (fdea850). Every clause is filled. Relative to `2366cce`, where every gate was re-run, d47b403 changes only `_spikes/s-t` and the ledger. The W0.4b rows were reproduced from their raw files with `median.py`, and by a fresh local run at d47b403.
Accepted by the W0.6 critic lane (critic-p0) with `CRITIC P0: APPROVE`; landed by the lead at 2026-09-25 20:10:37 JST (from `date`) with the critic's two wording fixes (A3: MiB values; A4: growth-call bytes). The AC-P6 time clause against a sonic-based naive comparator stays PENDING the owner (critic finding B6).

Row IDs refer to [`docs/perf/ledger.md`](perf/ledger.md); rulings refer to `.omc/handoffs/rulings.md`. G2 is the owner's W0.6 decision.

The verifier reproduced every number below from the committed raw files under `_spikes/*/results/`. Two scripts did the checking:

- `xcheck3.py` compared 859 cells of the W0.3 tables, allocation and timing, on (M) and (L).
- `xcheck2.py` compared 136 cells of the W0.5 tables.

The only differences are cells the ledger re-sources to the `duplicates` re-measure (W0.3-21/-22), plus one print artefact ("128.0 ns" against "128 ns").

A fresh run at `2366cce` on (M), under the lock with `GOEXPERIMENT=nosimd,noruntimesecret` and started 2026-09-25 19:43:46 JST, printed the same S-C1 CALL, STAGE, ITEM and MEM lines, and the same S-E1 and S-D1 allocation counts. Allocation counts are the same on (M) and (L) unless a row says otherwise.

The Verdict column compares each frozen value with the plan's text:

- **tightens**: the frozen value is stricter than the plan.
- **matches**: the frozen value equals the plan.
- **LOOSENS**: the frozen value is weaker than the plan. Every loosening below is owner-approved in G2.
- **PENDING**: no value is frozen yet.

| Criterion | Clause | Frozen value | Provenance | Plan text | Verdict |
| --- | --- | --- | --- | --- | --- |
| AC-P1 | steady-state encode allocations (NF1) | `E(kind) + B` per call on a warm pool, while the scratch stays within the 8 MiB ceiling. Up to 6 MiB on both hosts: bare `string` 2 (E 1 + B 1); boxed `any` 1; `json.RawMessage` 1; RawJSON appended verbatim 0. `*struct` 1 and flat `map[string]any` 2: at ≤ 1 MiB on both hosts, and at 6 MiB on (L). **At 6 MiB on (M): `*struct` 22 (27 601 168 B) and flat map 22 (27 597 808 B).** sonic grows the scratch past the ceiling on arm64 (9,020,409 B = 8.60 MiB and 9,181,884 B = 8.76 MiB after the call), so it is dropped after every call and every call pays growth (R22). A state holding m maps costs 1 + m (nested maps: 9 at 1 KiB up to 45 010 at 6 MiB, on both hosts; documented, a W5.3 candidate). At 9 MiB every kind exceeds the ceiling on both hosts and is reported, not budgeted | "S-E1 pooled encode" table, W0.3-04 (M) and W0.3-02 (L) (`ENC` lines of `s-e1/results/alloc-{M,L}.txt`); R22, R24, R24b | "≤ `E_sonic + B`, `E_sonic` = 1; B = 1 when the state is a bare `string`" | matches for string, boxed and RawMessage, and for `*struct` within the ceiling; tightens for RawJSON; **LOOSENS for map states: owner-approved (G2 b)**. `*struct` and flat-map states of about 6 MiB on arm64 are outside the steady-state budget (R22) |
| AC-P1 | encode bytes | warm-pool calls ≤ 112 B/call above sonic's own. Growth calls are recorded, not bounded: growing from a 4 KiB scratch, string, boxed, RawJSON and RawMessage cost 1.00–1.13 × body (the sequence's call 22, growing 6 → 9 MiB, costs 12,591,120 B = 1.33 ×, `s-e1/results/alloc-M.txt:78`); `*struct` and flat map about 4.3 × (M) and 5.2 × (L). On (M), a 6 MiB `*struct` or flat-map state never reaches the warm state (its scratch is dropped every call) and costs about 27.6 MB per call | W0.3-02 (L), W0.3-04 (M); R24b | "Bytes ≤ 1.05 × body + 4 KiB above sonic's own" | tightens on warm calls; **LOOSENS on growth calls: owner-approved (G2 b)** |
| AC-P1 | retained scratch | ceiling 8 MiB (`codec.ScratchCeiling = 8 << 20`; `TestBodyScratchCeiling`) | code at 2366cce | "scratch ≤ 8 MiB" | matches |
| AC-P1 | mixed-size sequence (`TestAllocScratchSequence`) | assertions start at call 2 (R22b). RawJSON state, calls 2..32: `0×9 \| 1 \| 0×10 \| 1 \| 1 \| 0×9`. Boxed-string state, calls 2..32: `1×9 \| 2 \| 1×10 \| 2 \| 2 \| 1×9`. Exactly one drop, at call 22; call 23 = base + 1. Call 1 is recorded: 4 and 8 | W0.3-02, W0.3-04; R22, R22b; fresh run identical | "exactly one scratch is dropped (call 22) … calls 12-21 and 24-32 match the single-size numbers"; "call 23 pays one extra allocation" | matches, scoped to RawJSON and boxed string. `*struct` and flat-map states on (M) also drop at call 11 and are recorded, not asserted (R22) |
| AC-P1 | g₆ / g₉ | asserted kinds: boxed 2 / 2, RawJSON 1 / 1. Recorded only: string 3 / 3; RawMessage 2 / 2; `*struct` 21 / 22 (M) and 30 / 32 (L); flat map 21 / 22 (M) and 31 / 33 (L) | "S-E1 growth" table (W0.3-02, W0.3-04) | "growth costs ≤ g₆/g₉ from S-E1" (plan probe: 5 / 7) | tightens |
| AC-P2 | decode allocations per fixture (a1) | result 4, type-last 4, **duplicates 17**, result-20 24, score-flood-mini 21, escaped-names 10, escaped-member-names 30, structured-legend 12, deviation-lone-surrogate 14, unknown-answer-type 1, parity-big-exp-unknown 1, no-answers 0, structured-legend-flood-1k 91, structured-legend-flood-10k 686. The ceiling of 8 on `result.json` stays in case W2.0's API wrapper adds allocations | W0.3-11 (L), W0.3-12 (M); duplicates: W0.3-21 (L), W0.3-22 (M), 825 B, 17/5336; R24, R24b; **duplicates = 17 owner-approved (G2 c)** | "plain 3-answer fixture ≤ 8 allocations (target 4), ≤ 0.5 × naive; 20-answer and escaped fixtures reported" | tightens: 4 meets the target, and the 20-answer and escaped fixtures now have budgets. "≤ 0.5 × naive" is PENDING W5.1 (the `naive` decoder) |
| AC-P4 | transport design | **option (iv-b) per G2, worded per R29 and R29b**: strict mode, the cold-start gate, and a header-write token. **There is one token per client transport**, not one per connection: a per-connection token would re-open F1, because the stdlib reserves the stream before `GotConn`. Each request acquires the token before `RoundTrip` and releases it at `WroteHeaders`, except that the first request on each **new** connection releases it at its response headers. That hold is bounded by the gate's wait bound, `connectTimeout` + `TLSHandshakeTimeout`: **20 s with both at their 10 s defaults** (R29b). After it, waiters fall through and the client still uses 1 connection. So K19 ("no gate state can wedge the client") holds under `WithNoTimeout`, and W2.2 tests the fall-through. K21 (stdlib-internal replays bypass the token) stays documented; W0.4b did not exercise it, because no stream was refused or replayed. K22 (new): each cold burst on a new connection pays the latency of one leader response | G2, R29, R29b, F1-c; W0.4 "Mitigations compared"; W0.4b results | §6.3 strict mode plus the gate | owner-approved design change |
| AC-P4 | cold 64 → 1 connection | 1 connection in 50/50 bursts on both hosts: with the gate (W0.4), and **with FirstHold, (iv-b), as frozen** (W0.4b). With 64 calls at 50 ms service time, FirstHold also opens 1 connection with 64/64 ok | W0.4-05 (L), W0.4-14 (M); **W0.4b-01 (L), W0.4b-03 (M)**; W0.4b-02 and -04 (`64vs0/strict/token-first/service50ms`) | "Cold 64 → 1 connection" | matches |
| AC-P4 | warm → 0 new connections | 0 in 50/50 on both hosts, with the gate and with FirstHold. The warm burst also keeps the original clause (every request on the wire before any response) in 50/50 | W0.4-05, W0.4-14; W0.4b-01, W0.4b-03 | "warm → 0" | matches |
| AC-P4 | 200 calls against MAX_CONCURRENT_STREAMS=8 all succeed within the per-call deadline on 1 connection (R29) | **Asserted**: 200/200 succeed within the 2 s per-call deadline, on 1 connection. Measured with FirstHold: 200 (200-200) ok, 1 (1-1) accept, wall **271.7 ms (L) and 293.0 ms (M)** (W0.4b); the W0.4 rows gave 271.5 and 291.8 ms. The server's stream high-water mark was 8, so the limit was reached. 64 vs 4 behaves the same: 64/64 on 1 connection, 176.0 ms (L) and 190.4 ms (M). **Recorded, not asserted**: the count of refused streams. It was 0 in all 20 W0.4b runs on each host and in the W0.4 matrix, but the loaded (M) stall runs (W0.4-16) saw 2 and 1 with no missed deadline, so an assertion of 0 would flake. Stall rate at a 500 ms deadline: 0 of 20 repetitions on both hosts. The broken strict configuration of the plan also shows 1 connection, but only 1 (1-3) (L) and 8 (1-8) (M) of the 200 succeed | **W0.4b-02 (L), W0.4b-04 (M)** (`s-t/results/{l,m}-st1b-firsthold.txt`); W0.4-06, -07, -15, -16 | "200 vs limit 8 → 1" | matches under (iv-b). R29's wording adds the success requirement, a tightening of the plan's text |
| AC-P4 | ordering | **Frozen, per G2 and R29 (4)**: no waiter's request is written before the leader's response headers; after them, all 63 waiters' requests are on the wire on the same connection before any waiter's response (5 s guard). Measured with FirstHold, 50 bursts per host (5 runs × 10): the clause held 50/50 on both hosts. Its parts held 50/50 each: (a) the first request at the handler is the leader's; (b) the leader's response arrives before any other HEADERS; (c) the guard released nothing; (d) every request is on connection 0. Margin for (b), from the leader's response to the first other HEADERS: at least 0.036 ms (L) and 0.016 ms (M). Negative control, the plain token (iv-a), passed only 37/50 (L) and 22/50 (M), so the check discriminates, but only by chance on loopback. **R29b/R29c: the frozen test delays the leader's response by at least 5 ms (W2.2 uses 20 ms) and asserts (b) from client-side traces (the leader's first response byte precedes every other caller's WroteHeaders); at 5 ms the plain-token control fails 50/50 on both hosts while FirstHold passes 50/50 (W0.4b-06/-08, fdea850).** Bounded hold (R29 (3), W0.4b-07/-09): with the gate wait bound at 500 ms and the leader's response held 1 s, the waiters' HEADERS go out at 500.9 ms (L) / 500.3 ms (M), 64/64 ok on 1 connection; without the bound they wait the full second. Cold-burst cost at 200 ms service (W0.4b-10): 202.7 vs 403.3 ms (L), 204.2 vs 405.2 ms (M): one leader response per cold burst (K22). W2.2 re-tests the fall-through against the production transport | **W0.4b-01 (L), W0.4b-03 (M)** (`s-t/results/{l,m}-st1-fanout-w04b.txt`); **W0.4b-06..10** (fdea850: `{l,m}-st1-fanout-w04b2.txt`, `{l,m}-st1b-firsthold2.txt`); `median.py` reproduces every count and latency; fresh local run at d47b403: FirstHold 10/10, control 7/10 | "every request is on the wire before any response" | **LOOSENS: owner-approved (G2 a); frozen** |
| AC-P4 | waiter latency (recorded, not asserted) | FirstHold, (iv-b), waiters' wire time p50 / p99: (L) 1.451 / 2.537 ms, (M) 1.064 / 1.928 ms; gate alone (W0.4): (L) 1.271 / 2.357 ms, (M) 1.146 / 1.898 ms. Leader's write to the first waiter's write, p50: 0.1 ms against 0.057 ms with the plain token (L), and 0.084 against 0.039 ms (M). **K22**: a cold burst's waiters wait for the leader's response. 64 calls at 50 ms service take 103.3 ms against 52.7 ms with the gate alone (L), and 103.9 against 53.2 ms (M) | W0.4b-01, -02, -03, -04; W0.4-05, W0.4-14 | "waiter p50/p99 recorded in the ledger, not asserted" | matches |
| AC-P5 | scope | bounds apply **per attempt**; `TestMemStatsCap` runs with `Retry(NoRetry())` and asserts cases (i)–(v) (R26b). The first read buffer (R27) is `min(Content-Length, 256 KiB)` for a declared body and 4 KiB, doubling, for an undeclared one | R26, R26b, R27 | "TotalAlloc delta for one call" | matches under NoRetry. R27's 4 KiB first buffer for undeclared bodies tightens small chunked calls (6 200 B against 264 248 B) |
| AC-P5 | (i) `Content-Length: 16 MiB`, 10 bytes sent → `io.ErrUnexpectedEOF` | TotalAlloc ≤ 327 680 B (256 KiB + 64 KiB). Measured 263 448 B, 15 mallocs; read stage 262 144 B | W0.5-01 (M), W0.5-05 (L); R26 | "< 1 MiB" | tightens |
| AC-P5 | (ii) declared 16 MiB + 1 → `*ResponseTooLargeError`, nothing read | ≤ 65 536 B. Measured 1 328 B, 15 mallocs; read stage 24 B | W0.5-01, W0.5-05; R26 | "refused before reading, delta < 1 MiB" | tightens |
| AC-P5 | (iii) undeclared 16 MiB + 1 → `*ResponseTooLargeError` at cap + 1 | ≤ 2 × cap + 65 536 B = 33 619 968 B. Measured under R27's policy: 33 551 664 B, 28 mallocs (margin 68 304 B) | W0.5-01 (M), W0.5-05 (L), `split-256K-4K` column; R26b | "≤ 2 × cap + 1 MiB" | tightens |
| AC-P5 | (iv) declared 16 MiB, accepted | ≤ 2 × cap + 65 536 B. Measured 33 294 392 B, 26 mallocs | W0.5-01, W0.5-05; R26 | – (new clause) | new clause (tightening) |
| AC-P5 | (v) undeclared 16 MiB, accepted | ≤ 2 × cap + 65 536 B. Measured under R27's policy: 33 552 440 B, 32 mallocs (margin 67 528 B); the largest delta under R27 | W0.5-01 (M), W0.5-05 (L), `split-256K-4K` column; R26b | – | new clause (tightening) |
| AC-P6 / NF3 | SDK allocations above the floor, N | **provisional N = 15** for q3: 3 questions, a 1 KiB boxed state, the Recorder, one attempt, no per-call options, no logger. Floor 8 (the Recorder's RoundTrip 7 + `E_sonic` 1); call/sdk 23/2 904; q20 SDK-own 35, recorded. W2.3 targets 12. A real transport adds 1 per attempt (the context's Done channel), not counted in N. **Re-frozen at W3.4** | W0.5-01 (M), W0.5-05 (L); R28 | "≤ N … (Rust: 12), provisional until the end of Phase 3; < 0.5 × naive allocations" | provisional (plan: N is set by S-C1). SDK-own / naive-own is 0.135 (q3) and 0.068 (q20), both < 0.5: matches |
| AC-P6 | time | `call/sdk` < `call/naive`: q3 4.647 against 8.968 µs (M), 5.837 against 16.76 µs (L); q20 23.28 against 39.78 µs (M), 24.25 against 77.34 µs (L). The comparator is `encoding/json` Marshal/Unmarshal into `map[string]any` (`_spikes/s-c1/naive.go`) | W0.5-04 (M), W0.5-08 (L) (`bench-*.txt`, `benchstat-*.txt`) | "`call/sdk` ns/op < `call/naive`" | matches against the `encoding/json` comparator; **against a sonic-based naive comparator: PENDING** the critic's final report and the owner |
| AC-P8 | time, 10⁴ vs 10³ flood | ratio ≤ 15. Measured for a1: `TestLinearity` 9.70–10.07 (M) and 9.98–10.26 (L); benchmark medians 10.09 (M, re-measured) and 9.75 (L) | W0.3-17 (L), W0.3-18 (M); W0.3-13 (L), W0.3-14 (M, re-measured at cc1524a per R25) | "≤ 15 × the 10³ flood" | matches |
| AC-P8 | lazy-pass allocations | ≤ 20 + ⌈members / 15⌉ + escaped keys iterated + 1 arena (c₀ = 20, c₁ = 1/15). Measured, arena included: 87 at 1 011 members and 682 at 10 011 (headroom 2 and 7); duplicates 10 at 18 members with 1 escaped key (bound 24) | W0.3-11, W0.3-12, W0.3-21, W0.3-22; R24 | "≤ c₀ + c₁ × (members visited), with c₀, c₁ frozen at W0.6" (probe: 82 / 677) | matches the plan's form, plus one allocation per escaped key |
| AC-P8 | allocation ratio, 10⁴ : 10³ | ≤ 12. Measured: 7.54 (whole decode, 91 → 686) and 7.92 (lazy pass, 86 → 681) | W0.3-11, W0.3-12 | "≤ 12" | matches |
| AC-Q3 | race and fuzz | `-race` green at d47b403: CI 36126929459 on 3 images; locally at 2366cce, 2026-09-25 19:43:39 JST (code outside `_spikes` has not changed since). The fuzz clause (4 targets, 60 s each in CI) joins at W6.1 | CI; local | "`-race` green; four fuzz targets 60 s each in CI" | in force (race); fuzz PENDING W6.1 |
| AC-Q4 | seams and D1 refusal | seam tests pass. The JIT files `[api.go rawmessage.go sonic.go]` compile. `vet` and `build` refuse with the D1 identifier on 386, riscv64, `-tags go1.28` and js/wasm. gotip refuses on tip | CI 36126929459 at d47b403 (lint; the same steps passed in 36113167206 at 2366cce); gotip 36111833315 (`gotip.yaml` unchanged since then); local, 2026-09-25 19:43:44 JST | AC-Q4 text | in force (matches) |

Notes:

1. **B1 is closed.**
   - 806f34b updates the spike's expected body, and `TestModuleDuplicates` passes for a1, a2 and b (local run at 2366cce).
   - Rows W0.3-21/-22 record `duplicates` at 17/5336 on both hosts; the raw files are `alloc-*-base31f2986.txt`.
2. **B2 is closed.** 93a4a9b tracks all four bench files, plus W0.5's two.
   - (L) files: identical to the surviving copies. `s-e1/results/bench-L.txt` has sha256 `d5ce80a8…`, and `s-d1/results/bench-L.txt` matches byte for byte (`cmp`).
   - Headers: every file's header matches its row (lock time, load, base).
   - W0.3-06 and W0.3-14 were re-measured at cc1524a with baseline ToolTags, and the rows note the original runs of record.
   - `_spikes/.gitignore`'s `!bench*.txt` beats the global `bench*.txt` rule: `git check-ignore -v` names `_spikes/.gitignore:5` for new bench files, while `bench.txt` stays ignored.
   - Every (M) and (L) timing cell of the W0.3 tables reproduces from these files.
